name: mehery_sender
description: A Flutter package to send device tokens to a server.
version: 0.1.8
homepage: https://github.com/mehery-soccom/mehery_sender_flutter.git

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.1'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 # For HTTP requests
  app_set_id: ^1.2.0 # For app-specific identification (if used)
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0
  flutter_local_notifications: ^22.0.0
  shared_preferences: ^2.2.2
  dynamix_flutter_sdk: ^0.0.3
  url_launcher: ^6.3.1
  device_info_plus: ^11.4.0
  package_info_plus: ^8.3.0
  web_socket_channel: ^2.4.0
  provider: ^6.0.0
  flutter_html: ^3.0.0
  webview_flutter: ^4.13.0
  super_tooltip: ^2.0.7
  webview_flutter_wkwebview: ^3.22.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# Flutter specific settings
flutter:
# You can add asset configurations if you plan to add assets in your package
# assets:
#   - images/example.png

# Fonts (if applicable)
# fonts:
#   - family: CustomFont
#     fonts:
#       - asset: fonts/CustomFont-Regular.ttf

  assets:
    - lib/android/res/layout/

false_secrets:
  - example/ios/Runner/GoogleService-Info.plist
